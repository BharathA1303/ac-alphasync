# Never commit real secrets or runtime state
.env
firebase-credentials.json

# Runtime dirs (created on the server)
uploads/
logs/
deploy/last_good_images.env

# Keep the template tracked
!.env.example
