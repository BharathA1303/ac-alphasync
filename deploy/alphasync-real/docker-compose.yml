# ============================================================================
#  AlphaSync Real — Production Docker Compose
#  Deploy path : /opt/apps/alphasync-real
#  Domain      : demo.alphasync.app
#  Project     : alphasync-real   (explicit — never autogenerated)
#
#  ISOLATION CONTRACT (multi-app server /opt/apps):
#    - Own project ....... alphasync-real
#    - Own network ....... alphasync-real-network
#    - Own volumes ....... alphasync-real-{pgdata,redis,uploads}
#    - Own containers .... alphasync-real-{frontend,backend,postgres,redis}
#    - Postgres & Redis are INTERNAL ONLY (no host port)
#    - Only 127.0.0.1:3001 (frontend) and 127.0.0.1:8001 (backend) published
#
#  Touches NOTHING belonging to any other application on the server.
# ============================================================================

name: alphasync-real

services:
  # ── Frontend (React build → container-internal nginx) ─────────────────────
  frontend:
    build:
      context: ./frontend
      args:
        VITE_FIREBASE_API_KEY: ${VITE_FIREBASE_API_KEY:-}
        VITE_FIREBASE_AUTH_DOMAIN: ${VITE_FIREBASE_AUTH_DOMAIN:-}
        VITE_FIREBASE_PROJECT_ID: ${VITE_FIREBASE_PROJECT_ID:-}
        VITE_FIREBASE_STORAGE_BUCKET: ${VITE_FIREBASE_STORAGE_BUCKET:-}
        VITE_FIREBASE_MESSAGING_SENDER_ID: ${VITE_FIREBASE_MESSAGING_SENDER_ID:-}
        VITE_FIREBASE_APP_ID: ${VITE_FIREBASE_APP_ID:-}
    image: alphasync-real-frontend:latest
    container_name: alphasync-real-frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - alphasync-real-network

  # ── Backend (FastAPI + Gunicorn/Uvicorn) ──────────────────────────────────
  backend:
    build:
      context: ./backend
    image: alphasync-real-backend:latest
    container_name: alphasync-real-backend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8001:8000"
    env_file:
      - .env
    environment:
      # Wire to THIS stack's internal services. Secrets come from .env only.
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      REDIS_URL: redis://redis:6379/0
      FIREBASE_CREDENTIALS_PATH: /app/firebase-credentials.json
    volumes:
      - alphasync-real-uploads:/app/uploads
      - ./firebase-credentials.json:/app/firebase-credentials.json:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "-s", "http://localhost:8000/api/health"]
      interval: 15s
      timeout: 8s
      retries: 10
      start_period: 90s
    networks:
      - alphasync-real-network

  # ── PostgreSQL (INTERNAL ONLY — no host port) ─────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: alphasync-real-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - alphasync-real-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - alphasync-real-network

  # ── Redis (INTERNAL ONLY — no host port) ──────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: alphasync-real-redis
    restart: unless-stopped
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru --save ""
    volumes:
      - alphasync-real-redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - alphasync-real-network

# ── Dedicated bridge network (owned solely by this app) ─────────────────────
networks:
  alphasync-real-network:
    name: alphasync-real-network
    driver: bridge

# ── Named volumes (explicit — never anonymous/autogenerated) ────────────────
volumes:
  alphasync-real-pgdata:
    name: alphasync-real-pgdata
  alphasync-real-redis:
    name: alphasync-real-redis
  alphasync-real-uploads:
    name: alphasync-real-uploads
